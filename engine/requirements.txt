pyyaml>=6.0
pytest>=7.4
weasyprint>=60      # Craft: render drafts to PDF (needs system pango/cairo; brew install weasyprint)
markdown>=3.5       # Craft: draft markdown body -> HTML for WeasyPrint
jinja2>=3.1         # Craft: structured cv_cover -> editorial template (templates/cv.html)
