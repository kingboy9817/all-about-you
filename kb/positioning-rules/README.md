# Positioning Rules

`voice.md` is currently wired into the engine through `provider.get_lens()`. The other files here (`claims-policy.md`, `presentation.md`, and `disclosure.md`) are policy documents for the user or AI assistant to include manually in drafting context until engine code reads them directly.
